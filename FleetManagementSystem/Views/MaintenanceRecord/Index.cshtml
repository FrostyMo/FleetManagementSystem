@*@model IEnumerable<FleetManagementSystem.Models.MaintenanceRecord>

<h2>Maintenance Records</h2>

<table class="table">
    <thead>
        <tr>
            <th>Vehicle</th>
            <th>Service Date</th>
            <th>Description</th>
            <th>Cost</th>
            <th>Service Provider</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var record in Model)
        {
            <tr>
                <td>@record.Vehicle.Name</td>
                <td>@record.ServiceDate.ToShortDateString()</td>
                <td>@record.Description</td>
                <td>@record.Cost</td>
                <td>@record.ServiceProvider</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@record.Id" class="btn btn-primary">Edit</a>
                    <a asp-action="Delete" asp-route-id="@record.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Create" class="btn btn-success">Add New Maintenance Record</a>*@